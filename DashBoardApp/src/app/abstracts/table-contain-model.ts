export class TableContainModel {
  id: number;
  firstName: string;
  lastName: string;  
  //constructor(_id: number,_firstName: string, _lastName: string, ) {
  //    this.id = _id;
  //    this.firstName= _firstName;
  //    this.lastName= _lastName; 
  //}

  //public isEqualObjects(item: TableContainModel) {
  //    return (this.id === item.id && this.firstName === item.firstName && this.lastName === item.lastName);
  //}
}

//TableContainModel.prototype.toString = function () {
//    return "id=" + this.id + " firstName=" + this.firstName + " lastName=" + this.lastName;
//}

